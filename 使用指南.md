# 合规审查知识库系统 - 使用指南

## 一、环境准备

### 1. 系统要求

- **操作系统**: macOS / Linux / Windows
- **Python**: 3.8 或更高版本
- **Node.js**: 16 或更高版本
- **Ollama**: 已安装并运行（用于AI模型）

### 2. 检查环境

```bash
# 检查Python版本
python --version  # 或 python3 --version

# 检查Node.js版本
node --version

# 检查Ollama是否运行
ollama list
```

### 3. Python虚拟环境配置（重要）

**为什么使用虚拟环境？**

虚拟环境可以为项目创建独立的Python环境，避免不同项目之间的依赖冲突。强烈建议使用虚拟环境来管理项目依赖。

#### 创建虚拟环境

```bash
# 进入后端目录
cd backend

# 创建虚拟环境（推荐使用venv）
python -m venv venv
# 或指定Python版本
python3 -m venv venv
```

#### 激活虚拟环境

**Windows:**
```bash
venv\Scripts\activate
```

**macOS / Linux:**
```bash
source venv/bin/activate
```

激活成功后，命令行前会显示 `(venv)` 前缀。

#### 安装项目依赖

```bash
# 确保虚拟环境已激活（看到 (venv) 前缀）
pip install -r requirements.txt
```

#### 退出虚拟环境

```bash
deactivate
```

#### 常见问题

- **找不到 python 命令**：Windows使用 `py` 或 `python3`，macOS/Linux使用 `python3`
- **虚拟环境激活后仍使用系统Python**：确认激活命令正确执行，检查是否有 `(venv)` 前缀
- **pip install 权限错误**：确保已激活虚拟环境，不要使用 `sudo`

### 4. 确认Ollama模型

确保已下载 `deepseek-r1:7b` 模型：

```bash
# 查看已下载的模型
ollama list

# 如果没有，下载模型
ollama pull deepseek-r1:7b
```

#### Ollama模型配置说明

**已配置的模型：**
- **大语言模型（LLM）**: `deepseek-r1:7b`
  - 用途：文档语义对比分析、生成标注说明
  - 已集成到系统中

- **嵌入模型（可选）**: `nomic-embed-text`
  - 用途：文档向量化，用于相似度计算和检索
  - 如果未下载，系统会使用备用方案

**配置环境变量：**

在 `backend/.env` 文件中确认以下配置：

```env
OLLAMA_BASE_URL=http://localhost:11434
OLLAMA_EMBEDDING_MODEL=nomic-embed-text  # 或您下载的其他嵌入模型
OLLAMA_LLM_MODEL=deepseek-r1:7b
```

**下载嵌入模型（可选，但推荐）：**

```bash
# 下载嵌入模型（用于文档向量化）
ollama pull nomic-embed-text

# 或者使用其他中文嵌入模型
ollama pull bge-large-zh
```

**测试Ollama连接：**

```bash
# 测试LLM模型
ollama run deepseek-r1:7b "你好"

# 测试嵌入模型（如果已下载）
ollama run nomic-embed-text "测试文本"
```

**模型使用说明：**

- **deepseek-r1:7b 模型特点**：
  - 模型类型：大语言模型（LLM）
  - 主要功能：文档语义理解和对比、生成合规问题说明、提供修改建议
  - 优势：支持中文、推理能力强、适合合规审查场景

- **注意事项**：
  - 模型响应时间：deepseek-r1:7b 是7B参数的模型，处理长文档可能需要一些时间
  - Token限制：系统会自动截断过长的文档内容（限制在3000字符左右）
  - 嵌入模型：如果未下载专门的嵌入模型，向量化功能可能受限，但不影响核心审查功能

## 二、快速开始

### 方式一：使用快速设置脚本（推荐）

#### macOS / Linux

```bash
# 1. 进入后端目录
cd backend

# 2. 运行快速设置脚本（首次使用）
chmod +x setup.sh
./setup.sh

# 3. 启动后端服务
./start.sh
```

#### Windows

```bash
# 1. 进入后端目录
cd backend

# 2. 运行快速设置脚本（首次使用）
setup.bat

# 3. 启动后端服务
start.bat  # 或手动运行: venv\Scripts\activate && python run.py
```

### 方式二：手动配置

#### 1. 配置后端

```bash
cd backend

# 创建虚拟环境
python -m venv venv

# 激活虚拟环境
# macOS/Linux:
source venv/bin/activate
# Windows:
venv\Scripts\activate

# 安装依赖
pip install -r requirements.txt

# 配置环境变量
cp env.example .env
# 编辑 .env 文件，确认Ollama配置

# 初始化数据库
python init_db.py

# 启动服务
python run.py
```

#### 2. 配置前端

```bash
cd frontend

# 安装依赖
npm install

# 启动开发服务器
npm run dev
```

## 三、访问系统

### 后端API

- **API文档**: http://localhost:8000/api/docs
- **健康检查**: http://localhost:8000/health
- **系统状态**: http://localhost:8000/api/v1/system/status

### 前端应用

- **访问地址**: http://localhost:5173

### 系统状态检查

在开始使用系统前，建议先检查系统状态，确保Ollama服务和AI模型正常运行：

1. **通过API检查**：
   ```bash
   curl http://localhost:8000/api/v1/system/status
   ```

2. **通过前端界面检查**：
   - 打开制度文件或员工文档上传页面
   - 点击"上传"按钮打开上传对话框
   - 对话框顶部会自动显示系统状态信息，包括：
     - Ollama服务状态（运行中/未运行）
     - LLM模型状态（模型名称/未找到）
     - 嵌入模型状态（如果已配置）
     - 状态提示消息

## 四、基本使用流程

### 步骤1: 上传制度文件

1. 打开浏览器，访问 http://localhost:5173
2. 点击顶部导航栏的 **"制度文件"**
3. 点击 **"上传制度文件"** 按钮
4. **检查系统状态**（重要）：
   - 上传对话框打开时，会自动显示系统状态
   - 确认Ollama服务状态为"运行中"
   - 确认LLM模型已就绪（显示模型名称，如 deepseek-r1:7b）
   - 如果状态异常，请先解决后再上传
5. 填写表单：
   - **制度名称**（必填）：如"财务管理制度"
   - **制度编号**（可选）：如"FIN-2024-001"
   - **制度类型**（可选）：选择类型，如"财务制度"
   - **生效日期**（可选）：选择日期
   - **上传人**（可选）：填写姓名
   - **文件**（必填）：选择PDF、Word、TXT或Markdown文件
6. 点击 **"上传"** 按钮
7. 等待上传完成，文件会自动解析并存储

### 步骤2: 上传员工文档

1. 点击顶部导航栏的 **"员工文档"**
2. 点击 **"上传文档"** 按钮
3. **检查系统状态**（重要）：
   - 上传对话框打开时，会自动显示系统状态
   - 确认Ollama服务和AI模型已就绪
   - 如果状态异常，请先解决后再上传
4. 填写表单：
   - **文档名称**（必填）：如"2024年度财务报告"
   - **文档类型**（可选）：如"报告"、"方案"、"申请"
   - **撰写人**（可选）：填写姓名
   - **部门**（可选）：填写部门名称
   - **文件**（必填）：选择要审查的文档
5. 点击 **"上传"** 按钮
6. 等待上传完成

### 步骤3: 创建审查任务

1. 点击顶部导航栏的 **"合规审查"**
2. 点击 **"创建审查任务"** 按钮
3. 选择审查配置：
   - **员工文档**：从下拉列表选择要审查的文档
   - **制度文件**：从下拉列表选择要对比的制度
   - **审查人**（可选）：填写审查人姓名
4. 点击 **"创建"** 按钮
5. 系统会在后台开始审查，状态显示为"处理中"

### 步骤4: 查看审查结果

1. 在 **"合规审查"** 页面，等待审查状态变为 **"已完成"**
2. 点击 **"查看详情"** 查看审查结果
3. 查看统计信息：
   - 不符合项总数
   - 严重项数量
   - 一般项数量
   - 轻微项数量

### 步骤5: 查看和管理标注

1. 点击顶部导航栏的 **"标注管理"**
2. 查看所有标注列表
3. 可以按以下条件筛选：
   - **严重程度**：严重、一般、轻微
   - **标注状态**：待确认、已确认、已忽略
4. 点击 **"查看"** 查看标注详情
5. 可以编辑标注状态和说明

## 五、功能详解

### 1. 制度文件管理

#### 功能
- ✅ 上传制度文件（PDF、Word、TXT、Markdown）
- ✅ 查看制度文件列表
- ✅ 搜索制度文件（按名称、编号）
- ✅ 按类型筛选
- ✅ 查看制度详情
- ✅ 编辑制度信息
- ✅ 删除制度文件
- ✅ 系统状态实时检查（上传时自动显示）

#### 使用技巧
- 上传时尽量填写完整的元数据，便于后续搜索和管理
- 制度编号建议使用统一的命名规范
- 定期清理不需要的制度文件
- **上传前注意检查系统状态**：确保Ollama服务和AI模型正常运行，避免上传后无法进行审查

### 2. 员工文档管理

#### 功能
- ✅ 上传员工文档
- ✅ 查看文档列表
- ✅ 按状态、作者、部门筛选
- ✅ 搜索文档
- ✅ 查看文档详情
- ✅ 编辑文档信息
- ✅ 删除文档
- ✅ 系统状态实时检查（上传时自动显示）

#### 使用技巧
- 上传时填写撰写人和部门，便于后续筛选
- 文档状态会自动更新：待审查 → 审查中 → 已完成
- **上传前注意检查系统状态**：确保Ollama服务和AI模型正常运行

### 3. 合规审查

#### 功能
- ✅ 创建审查任务
- ✅ 后台异步处理
- ✅ 自动对比分析
- ✅ 生成标注记录
- ✅ 统计信息展示

#### 审查流程
1. **创建任务**：选择文档和制度
2. **后台处理**：系统自动解析文档、调用AI对比
3. **生成标注**：识别不符合项并生成详细说明
4. **完成审查**：更新状态和统计信息

#### 注意事项
- 审查过程可能需要一些时间，取决于文档大小和AI处理速度
- 大文档会自动分块处理
- 审查结果会自动保存，可以随时查看

### 4. 标注管理

#### 标注类型
- **内容不符**：文档表述与制度要求不一致
- **缺失项**：制度要求但文档中缺失的内容
- **多余项**：文档中存在但制度未要求的内容
- **格式不符**：格式要求不符合制度规定

#### 严重程度
- **严重**：必须修改
- **一般**：建议修改
- **轻微**：可选修改

#### 标注状态
- **待确认**：系统自动生成的标注，等待人工确认
- **已确认**：人工确认的标注
- **已忽略**：忽略的标注

### 5. 系统状态监控

#### 功能
- ✅ 实时检查Ollama服务运行状态
- ✅ 检查LLM模型（deepseek-r1:7b）可用性
- ✅ 检查嵌入模型（nomic-embed-text）可用性（如果已配置）
- ✅ 在上传页面自动显示状态信息
- ✅ 提供清晰的状态提示消息

#### 状态指示说明
- **绿色（成功）**：服务/模型正常运行，可以正常使用
- **黄色（警告）**：Ollama服务运行中，但模型未找到，需要下载模型
- **红色（危险）**：Ollama服务未运行，需要启动服务

#### 使用场景
- **上传前检查**：在上传制度文件或员工文档时，系统会自动检查状态
- **问题诊断**：当审查任务失败时，可以通过状态检查快速定位问题
- **API检查**：通过 `/api/v1/system/status` 接口获取详细状态信息

## 六、常见问题

### Q1: 启动后端时提示端口被占用

**解决方案**：
```bash
# 使用改进后的启动脚本，会自动处理
./start.sh

# 或手动停止占用端口的进程
./stop.sh
```

### Q2: 上传文件失败

**可能原因**：
- 文件格式不支持（只支持PDF、Word、TXT、Markdown）
- 文件大小超过限制（默认50MB）
- 文件损坏

**解决方案**：
- 检查文件格式是否正确
- 检查文件大小
- 尝试重新上传

### Q3: 审查任务一直显示"处理中"

**可能原因**：
- AI模型处理时间较长
- Ollama服务未运行
- 文档过大

**解决方案**：
- 检查Ollama服务是否运行：`ollama list`
- 等待一段时间（大文档可能需要几分钟）
- 查看后端日志了解详细错误

### Q4: 文档解析失败

**可能原因**：
- PDF是扫描件（图片格式）
- Word文档格式异常
- 文件编码问题

**解决方案**：
- PDF扫描件需要使用OCR工具先转换
- 尝试将Word文档另存为新格式
- 文本文件确保使用UTF-8编码

### Q5: 前端无法连接后端

**解决方案**：
- 确认后端服务已启动（访问 http://localhost:8000/health）
- 检查前端代理配置（vite.config.ts）
- 检查浏览器控制台错误信息

### Q6: Ollama模型调用失败

**解决方案**：
```bash
# 检查Ollama服务
ollama list

# 确认模型已下载
ollama pull deepseek-r1:7b

# 测试模型
ollama run deepseek-r1:7b "你好"
```

**通过系统状态检查**：
1. 访问 http://localhost:8000/api/v1/system/status 查看详细状态
2. 或在上传页面查看状态提示
3. 根据状态信息进行相应处理：
   - 如果Ollama服务未运行：启动Ollama服务
   - 如果模型未找到：下载对应模型

### Q7: 如何检查系统是否就绪？

**解决方案**：
1. **通过前端界面**：
   - 打开制度文件或员工文档上传页面
   - 点击"上传"按钮，查看对话框顶部的状态显示
   - 确认所有状态为绿色（成功）

2. **通过API接口**：
   ```bash
   curl http://localhost:8000/api/v1/system/status
   ```
   返回示例：
   ```json
   {
     "ollama_service": true,
     "llm_model": true,
     "embedding_model": false,
     "llm_model_name": "deepseek-r1:7b",
     "message": "系统就绪，可以正常使用"
   }
   ```

3. **通过命令行**：
   ```bash
   # 检查Ollama服务
   ollama list
   
   # 应该能看到 deepseek-r1:7b 模型
   ```

## 七、高级功能

### 1. 批量上传

目前支持单个文件上传，批量上传功能正在开发中。

### 2. 审查历史

在"合规审查"页面可以查看所有历史审查记录。

### 3. 标注导出

标注导出功能正在开发中，将支持PDF、Word、JSON格式。

### 4. 统计分析

统计分析功能正在开发中，将提供图表展示。

## 八、系统维护

### 备份数据

```bash
# 备份数据库
cp backend/data/compliance.db backend/data/compliance.db.backup

# 备份上传的文件
tar -czf uploads_backup.tar.gz backend/data/uploads/
```

### 清理数据

- 删除不需要的制度文件和员工文档
- 定期清理过期的审查记录
- 清理临时文件

### 日志查看

后端日志会显示在终端中，包括：
- 文件上传日志
- 文档解析日志
- AI处理日志
- 错误信息

## 九、技术支持

### 查看文档

- **系统需求文档**: `系统需求文档.md`
- **功能实现总结**: `功能实现总结.md` - 查看已实现功能的详细说明
- **功能更新说明**: `功能更新说明.md` - 了解最新功能更新
- **后端README**: `backend/README.md`
- **前端README**: `frontend/README.md`
- **Ollama配置说明**: `backend/OLLAMA配置说明.md`

### 检查系统状态

```bash
# 检查后端服务
curl http://localhost:8000/health

# 检查系统状态（包括Ollama和模型）
curl http://localhost:8000/api/v1/system/status

# 检查可用模型列表
curl http://localhost:8000/api/v1/system/models

# 检查Ollama服务
ollama list

# 检查数据库
# 使用SQLite工具查看 backend/data/compliance.db
```

## 十一、最佳实践

1. **系统启动前检查**
   - 确保Ollama服务已启动
   - 确认所需模型已下载（deepseek-r1:7b）
   - 通过系统状态API验证服务就绪

2. **制度文件管理**
   - 使用统一的命名规范
   - 及时更新制度版本
   - 定期清理过期制度

3. **文档上传**
   - **上传前检查系统状态**（重要）
   - 上传前检查文档格式
   - 填写完整的元数据
   - 确保文档内容清晰可读

4. **审查流程**
   - 选择合适的制度进行对比
   - 等待审查完成后再查看结果
   - 及时处理标注信息
   - 如审查失败，先检查系统状态

5. **系统维护**
   - 定期备份数据
   - 监控系统日志
   - 及时更新依赖包
   - 定期检查Ollama服务和模型状态

---

## 十二、功能实现说明

### 已完成的功能

#### 1. 文件上传功能 ✅

**后端实现：**
- 文件服务 (`app/services/file_service.py`)
  - 文件验证（格式、大小）
  - 文件存储路径生成
  - 文件保存和删除
  - 支持格式：PDF、Word(.docx)、TXT、Markdown

**前端实现：**
- 文件选择组件（Element Plus Upload）
- 表单验证
- 上传进度提示
- 错误处理

#### 2. 文档解析功能 ✅

**文档解析器** (`app/services/document_parser.py`)：
- PDF解析（使用pdfplumber和PyPDF2）
- Word文档解析（.docx格式）
- 文本文件解析（TXT、Markdown）
- 自动编码检测

**解析流程：**
- 上传时自动解析文档内容
- 内容存储到数据库
- 支持后续审查使用

#### 3. 知识库管理功能 ✅

**制度文件管理：**
- 上传制度文件
- 列表查询（支持分页、搜索、筛选）
- 查看详情
- 更新元数据
- 删除（软删除）

**员工文档管理：**
- 上传员工文档
- 列表查询（支持分页、搜索、筛选）
- 查看详情
- 更新信息
- 删除（软删除）

#### 4. Ollama集成 ✅

**Ollama服务** (`app/services/ollama_service.py`)：
- 文档对比分析（使用deepseek-r1:7b）
- 标注信息生成
- 嵌入向量化（可选）

#### 5. 合规审查核心功能 ✅

**审查服务** (`app/services/review_service.py`)：
- 文档内容提取
- 使用Ollama进行语义对比
- 自动识别不符合项
- 生成标注记录
- 统计信息计算

**审查流程：**
1. 创建审查任务
2. 后台异步处理
3. 文档解析（如未解析）
4. AI对比分析
5. 生成标注
6. 更新状态和统计

### 技术要点

**文件存储：**
- 按类别和日期组织文件
- 使用UUID生成唯一文件名
- 存储相对路径到数据库
- 支持文件下载

**数据库设计：**
- 软删除机制
- 内容缓存（解析后的文本）
- 文件元数据管理

**后台任务：**
- 使用FastAPI BackgroundTasks
- 独立的数据库会话
- 错误处理和状态更新

**错误处理：**
- 文件验证（格式、大小）
- 解析失败处理
- 数据库事务回滚
- 文件清理（失败时删除已上传文件）

### 待完善功能

**标注功能：**
- [ ] 标注详情查看页面
- [ ] 标注编辑功能
- [ ] 标注导出（PDF、Word、JSON）
- [ ] 标注可视化展示

**高级功能：**
- [ ] 批量审查
- [ ] 审查历史记录
- [ ] 统计分析图表
- [ ] 文档预览功能

## 十三、功能更新说明

### 系统状态监控功能（最新更新）

#### 新增功能

**1. 系统状态API**

新增了系统状态检查接口，可以实时检查：
- Ollama服务运行状态
- LLM模型（deepseek-r1:7b）可用性
- 嵌入模型（nomic-embed-text）可用性

**API接口：**
- `GET /api/v1/system/status` - 获取系统状态
- `GET /api/v1/system/models` - 获取可用模型列表

**2. 上传页面模型状态显示**

在制度文件上传页面和员工文档上传页面的上传对话框中添加了模型状态显示：
- 打开上传对话框时自动检查模型状态
- 显示Ollama服务状态
- 显示LLM模型状态
- 显示嵌入模型状态（如果可用）
- 显示状态提示信息

#### 状态显示说明

**状态指示器：**
- **绿色（成功）**：服务/模型正常运行
- **黄色（警告）**：模型未找到但服务运行中
- **红色（危险）**：服务未运行

**使用场景：**
- **场景1：正常使用** - 所有状态为绿色，系统就绪
- **场景2：模型未就绪** - Ollama运行但模型未找到，需要下载模型
- **场景3：服务未运行** - Ollama服务未运行，需要启动服务

#### 技术实现

**后端：**
- 新增 `app/api/v1/system.py` 模块
- 使用 `requests` 库检查Ollama API
- 支持模型名称模糊匹配（支持带版本号的模型名）

**前端：**
- 新增 `src/api/system.ts` API封装
- 在 `Regulations.vue` 和 `Documents.vue` 中添加状态显示
- 使用 `watch` 监听对话框打开，自动检查状态
- 使用 Element Plus 的 `el-alert` 和 `el-tag` 组件显示状态

#### 优势

1. **实时反馈**：用户在上传前就能知道系统是否就绪
2. **问题诊断**：快速定位问题（服务未运行 vs 模型未找到）
3. **用户体验**：避免上传后才发现无法进行审查
4. **可视化**：清晰的状态指示，一目了然

## 十四、故障排查指南

### Swagger UI 加载失败问题

#### 问题现象
- `Failed to load resource: net::ERR_CONNECTION_TIMED_OUT`
- `SwaggerUIBundle is not defined`

#### 可能原因
1. **网络问题**：无法访问Swagger UI的CDN资源
2. **防火墙/代理**：阻止了外部资源加载
3. **浏览器缓存**：旧的缓存导致问题

#### 解决方案

**方案1：使用ReDoc（推荐，最简单）**

ReDoc是另一个API文档工具，通常更稳定：

访问地址：http://localhost:8000/api/redoc

ReDoc不依赖外部CDN，应该可以正常加载。

**方案2：清除浏览器缓存并刷新**

1. 按 `F12` 打开开发者工具
2. 右键点击浏览器刷新按钮
3. 选择 **"清空缓存并硬性重新加载"**
4. 或者使用 **无痕模式**（Ctrl+Shift+N / Cmd+Shift+N）

**方案3：检查网络连接**

```bash
# 测试CDN连接
curl -I https://cdn.jsdelivr.net

# 如果无法连接，可能是网络问题
```

**方案4：使用API直接测试（无需Swagger UI）**

如果Swagger UI无法使用，可以直接使用curl或Postman测试API：

```bash
# 健康检查
curl http://localhost:8000/health

# 获取制度文件列表
curl http://localhost:8000/api/v1/regulations/

# 获取员工文档列表
curl http://localhost:8000/api/v1/documents/
```

**方案5：使用前端应用（推荐）**

**最佳方案**：直接使用前端应用，不需要Swagger UI！

1. 启动前端：`cd frontend && npm run dev`
2. 访问：http://localhost:5173
3. 前端已经集成了所有功能，比Swagger UI更好用

**为什么推荐使用前端应用？**

前端应用（http://localhost:5173）提供了：
- ✅ 完整的用户界面
- ✅ 文件上传功能
- ✅ 数据可视化
- ✅ 更好的用户体验
- ✅ 不依赖外部CDN

**Swagger UI只是用于API测试和文档查看，实际使用应该通过前端应用！**

### 端口被占用问题

#### 问题现象
启动后端时提示端口8000被占用

#### 解决方案

```bash
# 使用改进后的启动脚本，会自动处理
cd backend
./start.sh

# 或手动停止占用端口的进程
./stop.sh

# 查看占用端口的进程（macOS/Linux）
lsof -ti:8000

# 查看占用端口的进程（Windows）
netstat -ano | findstr :8000
```

### 前端连接问题

#### 问题：前端无法连接后端

**检查清单：**

1. **后端服务运行**
   ```bash
   curl http://localhost:8000/health
   ```

2. **前端代理配置**
   检查 `frontend/vite.config.ts` 中的代理配置

3. **CORS配置**
   检查 `backend/app/main.py` 中的CORS设置

4. **浏览器控制台**
   查看具体的错误信息

**解决方案：**

```bash
# 重启后端
cd backend
./stop.sh
./start.sh

# 重启前端
cd frontend
npm run dev
```

### Ollama连接问题

#### 问题：AI功能无法使用

**检查步骤：**

```bash
# 1. 检查Ollama服务
ollama list

# 2. 测试模型
ollama run deepseek-r1:7b "你好"

# 3. 检查配置
cat backend/.env | grep OLLAMA
```

**解决方案：**

```bash
# 启动Ollama服务（如果未运行）
ollama serve

# 下载模型（如果缺失）
ollama pull deepseek-r1:7b

# 通过系统状态API检查
curl http://localhost:8000/api/v1/system/status
```

**常见问题：**

- **无法连接到Ollama**：
  - 确认Ollama服务正在运行: `ollama serve`
  - 检查端口11434是否被占用
  - 确认 `OLLAMA_BASE_URL` 配置正确

- **模型不存在**：
  ```bash
  # 确认模型已下载
  ollama list
  
  # 如果未下载，执行
  ollama pull deepseek-r1:7b
  ```

- **响应速度慢**：
  - 这是正常现象，7B模型处理需要时间
  - 可以考虑使用更小的模型（如果可用）
  - 或者优化提示词，减少输入长度

### 文件上传问题

#### 问题：文件上传失败

**检查清单：**

1. **文件大小**：是否超过50MB限制
2. **文件格式**：是否支持（PDF、Word、TXT、Markdown）
3. **存储目录**：`backend/data/uploads/` 是否存在且有写权限

**解决方案：**

```bash
# 检查目录权限
ls -la backend/data/uploads/

# 创建目录（如果不存在）
mkdir -p backend/data/uploads
chmod 755 backend/data/uploads
```

### 数据库问题

#### 问题：数据库操作失败

**检查步骤：**

```bash
# 检查数据库文件
ls -la backend/data/compliance.db

# 重新初始化
cd backend
source venv/bin/activate  # 或 venv\Scripts\activate (Windows)
python init_db.py
```

### 模块未找到问题

#### 错误：ModuleNotFoundError

**解决方案：**

```bash
# 重新安装依赖
cd backend
source venv/bin/activate  # 确保虚拟环境已激活
pip install -r requirements.txt
```

### 日志查看

**后端日志：**

后端日志会直接显示在终端中，包括：
- 请求日志
- 错误信息
- 处理时间

**前端日志：**

打开浏览器开发者工具（F12）查看：
- Console：JavaScript错误
- Network：API请求状态

### 快速检查清单

遇到问题时，按以下顺序检查：

- [ ] 后端服务是否运行？`curl http://localhost:8000/health`
- [ ] 前端服务是否运行？访问 http://localhost:5173
- [ ] Ollama服务是否运行？`ollama list`
- [ ] 模型是否已下载？`ollama list` 应该能看到 deepseek-r1:7b
- [ ] 系统状态是否正常？`curl http://localhost:8000/api/v1/system/status`
- [ ] 虚拟环境是否激活？命令行前应该有 `(venv)` 前缀
- [ ] 依赖是否安装？`pip list` 查看已安装的包
- [ ] 数据库是否初始化？检查 `backend/data/compliance.db` 是否存在

---

**祝您使用愉快！如有问题，请查看相关文档或检查系统日志。**

